// time complexity is the time tsken n=by the alagorithm to  run as a function of length of the input
// space complexity is the  amount of memory ussed by an algorithm as a function of length of the input 
//notations ->   bigO(O)->worst 
// omega(Ω)->best
// theta(θ)->average
// O(1) constant time complexity            (less ccomplex)
// O(log n) logarithmic time complexity             ^
// O(n) linear time complexity                      |
// O(n log n) linearithmic time complexity          |
// O(n^2) quadratic time complexity                 |
// O(2^n) exponential time complexity          (more complex)

#include<iostream>
using namespace std;
 int main(){

 }